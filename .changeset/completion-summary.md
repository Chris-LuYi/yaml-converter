---
"yaml-converter": patch
---

Add completion summary: row counts and output paths printed after every successful conversion. JSON mode includes `rows` (YAML→Excel) or `sheets`/`totalRows` (Excel→YAML) fields.
