---
"yaml-converter": minor
---

Add `--recursive`, `--merge`, and `--split-by` flags for multi-project agent pipeline support. Merge flattens a directory of YAML files into one Excel sheet with an injected tag column; split reverses the operation by grouping rows back into separate YAML files by field value.
